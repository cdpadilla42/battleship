const events = () => {
  const addListeners = (player, playerName) => {
    const boardData = player.playerBoard.showBoard();
    const boardElement = document.querySelector(`.${playerName}table`);
  };
};
